/*
let userName -> camelCase
let user_name -> snake_case
let user-name -> kebab-case
let UserName -> PascalCase

var: variable global, problema de redefinición, no se usa.
let: variable local, se usa más a menudo.
const: variable constante, no se puede redefinir. Es lo mas usado.

*/